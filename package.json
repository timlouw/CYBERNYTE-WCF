{
  "type": "module",
  "scripts": {
    "start": "bun framework/compiler/build.ts dev client serve",
    "start-prod": "bun framework/compiler/build.ts prod client serve",
    "build": "bun framework/compiler/build.ts dev client",
    "build-prod": "bun framework/compiler/build.ts prod client",
    "benchmark": "bun lighthouse.js --serve",
    "format": "bunx --bun prettier --write \"**/*.{ts,json,html}\"",
    "format:check": "bunx --bun prettier --check \"**/*.{ts,json,html}\"",
    "prepare": "husky"
  },
  "devDependencies": {
    "esbuild": "^0.27.1",
    "husky": "^9.1.7",
    "lighthouse": "^13.0.1",
    "typescript": "^5.9.3"
  }
}
